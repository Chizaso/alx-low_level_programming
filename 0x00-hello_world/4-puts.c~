#!/bin/bash
#include <stdio.h>

/**
 * main - prints the phrase "Programming is like building a multilingual puccle
 * Return: 0 if exited properly
 */
int main(void)
{
	puts("\"Programming is like building a multilingial puzzle");
	return (0);

}
